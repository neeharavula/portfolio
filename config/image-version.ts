/* Image version for cache busting */

// Increment this version number whenever you update images in Vercel Blob
// Forces browsers to fetch new images instead of using cached versions
export const IMAGE_VERSION = "v1";
